import * as React from 'react';
import Slider from '../slider/Slider.jsx';

require('./app.css');

const App = props => (
    <div className="canvas">
        <Slider />
    </div>
);

export default App;